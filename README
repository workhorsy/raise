
Raise is a small build automation tool that ships with your software.

The project is hosted at https://github.com/workhorsy/raise and licensed under
a MIT style license.

// Linux/Unix install requirements
Python 2.6 or greater
stty

// Windows with Cygwin install requirements
Python 2.6 or greater
ncurses
stty

// OS X install requirements
Python 2.6 or greater
stty

// The test suite can be run with
cd tests
python test_suite.py
sudo python root_test_suite.py


// Examples can be run from the example directory
cd examples/c_simple/
./raise test


// Documentation can be regenerated in the docs directory
sudo python generate_documentation.py
